<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="line">
<!--
    画线注意： 1、只能画水平线，不能画竖直线
              2、线高通过stroke的width设定
              3、size的hight定义整个形状区域的高度，所以要大于stroke的width
              4、线是居中显示的，左右会有留白区域
              5、引用虚线的view需要添加属性android:layerType，值设为"software"，否则显示不了虚线。
-->
    <stroke
        android:width="1dp"
        android:color="@color/colorPrimary"
        android:dashGap="6dp"
        android:dashWidth="2dp"/>

    <size
        android:height="2dp"/>
</shape>